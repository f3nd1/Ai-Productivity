# Copy to server/.env and fill in real values. Never commit the filled-in .env.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
OPENAI_API_KEY=
# Optional: port the Express server listens on (default 3001)
PORT=3001
